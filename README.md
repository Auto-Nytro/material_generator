# Material Generator
A pure CSS implementation of Material Design 3.

All Material Design 3 styles are represented as strongly-typed TypeScript values which are then fed to a generator to output CSS files.

# Status
This library is a work in progress. Check out the "dev" branch to see the source code.
